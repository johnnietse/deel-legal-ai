# Kubernetes Service for Deel Lab Legal AI API
apiVersion: v1
kind: Service
metadata:
  name: legal-ai-api-service
  labels:
    app: legal-ai-api
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: legal-ai-api
