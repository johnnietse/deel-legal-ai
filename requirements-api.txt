# OpenJustice.ai - API Production Dependencies (lightweight, no torch/transformers)
python-dotenv>=1.0.0
requests>=2.31.0

# API Server
fastapi>=0.109.0
uvicorn>=0.25.0
pydantic[email]>=2.5.0
python-multipart>=0.0.9

# Authentication
python-jose[cryptography]>=3.3.0
bcrypt>=4.1.3,<5.0
google-auth>=2.29.0

# SSE Streaming
sse-starlette>=2.0.0
anyio>=4.3.0

# Vector Database (Pinecone primary + Milvus fallback)
pinecone>=6.0.2,<7.0
pymilvus>=2.4.0

# Sparse Retrieval — BM25 (Elasticsearch backend)
elasticsearch>=8.18.0,<9.0
rank-bm25>=0.2.2

# PostgreSQL (Neon)
sqlalchemy[asyncio]>=2.0.30
asyncpg>=0.29.0

# Text Processing
tiktoken>=0.5.0

# ML Classifier (imported by API at startup)
scikit-learn>=1.3.0
joblib>=1.3.0
pandas>=2.1.0

# Knowledge Graph
networkx>=3.2.0

# Utilities
tqdm>=4.66.0
tenacity>=8.2.0
